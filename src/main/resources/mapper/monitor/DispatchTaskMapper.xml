<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.gms.mapper.monitor.DispatchTaskMapper">

    <select id="getUnitByVehicleId" parameterType="integer" resultType="com.zs.gms.entity.monitor.DispatchTask">
        select tdt.*
        from t_dispatch_task tdt
                 inner join t_task_rule ttr on tdt.UNITID = ttr.UNITID
        where ttr.VEHICLEID = #{vehicleId}
          and ttr.STATUS not in ('3', '5')
          and tdt.STATUS != '0'
    </select>
</mapper>